/** Re-export shared settings for renderer */
export type {
  PlayerColorId,
  RadarSettings,
  SideBarSettings,
  AudioSettings,
  AppSettings,
} from '@shared/settings';
export {
  PLAYER_COLOR_MAP,
  DEFAULT_RADAR_SETTINGS,
  DEFAULT_SIDEBAR_SETTINGS,
  DEFAULT_AUDIO_SETTINGS,
  DEFAULT_SETTINGS,
} from '@shared/settings';
